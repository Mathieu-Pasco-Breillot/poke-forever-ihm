<Page
    x:Class="PokeForeverIHM.Pages.Pokedex"
    xmlns:uControl="using:PokeForeverIHM.UsersControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PokeForeverIHM.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="140"/>
        </Grid.ColumnDefinitions>
        <Pivot x:Name="PokePivot" ItemsSource="{Binding}" Background="Black">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <StackPanel Width="100%" HorizontalAlignment="Stretch">
                        <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                        <TextBlock Text="{Binding Level}" Foreground="White" FontSize="12"/>
                    </StackPanel>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <Pivot.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="25"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="80%"/>
                        </Grid.RowDefinitions>
                        <Image Source="{Binding GetImageType}" Width="25" Height="25" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                        <Image Source="{Binding GetImage}" Grid.ColumnSpan="2"/>
                        <TextBlock Text="{Binding Abstract}" Margin="10,0,0,0" Foreground="White" FontSize="14"  HorizontalAlignment="Stretch" Grid.ColumnSpan="2" Grid.Row="1" TextWrapping="WrapWholeWords"/>
                    </Grid>
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>
        <uControl:MenuPanel Grid.Column="1"/>
    </Grid>
</Page>
